<nav class="navbar navbar-expand-lg eventures-bg-colour">
    <a class="navbar-brand" href="/">Eventures</a>
    <div class="collapse navbar-collapse d-flex justify-content-between" id="navbarNav">
        <ul class="navbar-nav">
            <li class="nav-item">
                <a class="nav-link eventures-bg-colour" href="/">Home</a>
            </li>
            @if (this.User.Identity.IsAuthenticated)
            {
                <li class="dropdown">
                    <a class="nav-link dropdown-toggle eventures-bg-colour" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Events
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" asp-controller="Events" asp-action="AllEvents">All Events</a>

                        @if (User.IsInRole("Admin"))
                        {
                            <a class="dropdown-item" asp-controller="Events" asp-action="Create">Create event</a>
                        }
                        else
                        {
                            <a class="dropdown-item" asp-controller="Events" asp-action="MyEvents">My Events</a>
                        }
                    </div>
                </li>
                @if (User.IsInRole("Admin"))
                {
                    <li class="dropdown">
                        <a class="nav-link dropdown-toggle eventures-bg-colour" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Orders
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" asp-controller="Orders" asp-action="AllOrders">All Orders</a>
                        </div>
                    </li>
                }
            }
        </ul>
    </div>
    <div class="navbar-nav left">
        <ul class="navbar-nav">
            @if (User.Identity.IsAuthenticated)
            {
                <li class="nav-item">
                    <a class="nav-link eventures-bg-colour">Hello, @User.Identity.Name</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link eventures-bg-colour" asp-controller="Users" asp-action="Logout">Logout</a>
                </li>
            }
            else
            {
                <li class="nav-item">
                    <a class="nav-link eventures-bg-colour" href="/users/register">Register</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link eventures-bg-colour" href="/users/login">Login</a>
                </li>
            }
        </ul>
    </div>
</nav>
